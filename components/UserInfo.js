export default class UserInfo {
  constuctor({ nameElementSelector, metierElementSelector }) {
    this._nameElementSelector = nameElementSelector;
    this._metierElementSelector = metierElementSelector;
  }
}
